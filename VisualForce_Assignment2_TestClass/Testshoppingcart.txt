@isTest(SeeAllData=false)
public class Testshopping {

    @isTest(SeeAllData=true) static void testInitialPurchaseOrderList() {
        shopping s= new shopping();
        List<PurchaseOrder__c> testpurchaseorderlist= new List<PurchaseOrder__c>();
        for(PurchaseOrder__c purchaseorder: [SELECT Total_Amount__c,Order_Status__c FROM PurchaseOrder__c]) {
            testpurchaseorderlist.add(purchaseorder);
        }
        System.assertEquals(true,s.purchaseorderlist==testpurchaseorderlist);
    }
}